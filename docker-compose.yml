version: '3.8'
services:
  opaochatplus:
    image: oneperfect/opaochat:opaochatplus
    restart: always
    container_name: opaochatplus
    ports:
      - "34568:34568"
    volumes:
      - ./.env:/app/.env
      - ./data:/app/data
