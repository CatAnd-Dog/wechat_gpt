version: '3.8'
services:
  opaochatqiwei:
    image: oneperfect/opaochatqiwei:latest
    restart: always
    container_name: opaochatqiwei
    ports:
      - "34568:34568"
    volumes:
      # - ./.env:/app/.env
      - ./data:/app/data
    environment:
      WX_TOKEN: 
      APPID: 
      APPSECRET: 
      CHAT_URL: 
      APIKEY: 