version: '3.8'
services:
  opaochatplus:
    image: oneperfect/opaochatplus:latest
    restart: always
    container_name: opaochatplus
    ports:
      - "34568:34568"
    volumes:
      # - ./.env:/app/.env
      - ./data:/app/data
    environment:
      WX_TOKEN: 
      APPID: 
      APPSECRET: 
      CHAT_URL: 
      APIKEY: 
